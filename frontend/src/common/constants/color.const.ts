import { ROLE } from './default.const'

export const RoleBadgeColor = {
  [ROLE.ADMIN]: 'bg-gradients-danger',
  [ROLE.EVENT_PROVIDER]: 'bg-gradients-warning',
  [ROLE.USER]: 'bg-gradients-success',
}
